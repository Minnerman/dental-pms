import { test } from "@playwright/test";

test.describe("clinical odontogram completed treatment findings scout", () => {
  test.skip(
    true,
    "Stage 163F scout scaffold only: enable after completed_treatment_findings import/parity is implemented."
  );

  test("placeholder for real-patient findings proof", async () => {
    // Intentionally empty in scouting stage.
  });
});
