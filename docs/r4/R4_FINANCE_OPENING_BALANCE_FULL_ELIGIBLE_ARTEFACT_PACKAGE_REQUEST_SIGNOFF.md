# R4 Finance Opening-Balance Full Eligible-Row Artefact Package Request Sign-Off

Status date: 2026-05-09

Sign-off baseline: `origin/master@bd686b00d209771dabdb39dc82629bddd5cf6a7c`

This record preserves explicit owner sign-off for the package request/template
only:

- `docs/r4/R4_FINANCE_OPENING_BALANCE_FULL_ELIGIBLE_ARTEFACT_PACKAGE_REQUEST.md`

The sign-off accepts that request/template as the required structure for any
future full eligible-row opening-balance artefact package request.

## Scope

This sign-off is limited to the package request/template document. It does not
include a full eligible-row artefact and does not satisfy any later artefact
creation, artefact access, provenance, redaction/storage, validation/no-write,
guarded apply/write, live import, or production execution gate.

Any future full eligible-row artefact package remains a separate explicitly
authorised slice and must follow the approved provenance/redaction governance
and package request gates.

`finance_import_ready=false`. Migration/import is not complete. Live finance
import is not authorised. Production readiness is not established. Full
eligible-row artefact execution has not happened.

## Non-Authorisations

This sign-off does not authorise:

- R4 access;
- real R4 artefact access;
- artefact creation;
- artefact copying;
- artefact hashing;
- artefact storage;
- artefact validation;
- artefact execution;
- real patient data use;
- PMS DB connection;
- guarded scratch apply;
- CLI validation/no-write;
- live/default PMS DB writes;
- actual PMS Postgres writes;
- finance import;
- invoice, payment, or staging import;
- production execution;
- full eligible-row artefact execution.

## Data Handling Boundary

This sign-off record contains no patient names, DOBs, addresses, phone numbers,
emails, clinical details, unredacted DSNs or secrets, or full real artefact
contents.
